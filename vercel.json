{
  "installCommand": "npm install --ignore-scripts && npm install --prefix server --ignore-scripts && npm install --prefix client --ignore-scripts",
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/[...path]" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}